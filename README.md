# FFacts — забавные факты, костыли и легенды IT

Локальная HTML-страница со 120+ смешными и историческими фактами про интернет, веб, железо, разработку и игры. Поддерживает фильтр по тексту, выбор категорий, показ случайного факта и переключение светлой/тёмной темы. Всё работает офлайн, без сборки и зависимостей.

## Структура
- `index.html` — разметка и подключение статики.
- `style.css` — оформление, темы, сетки, карточки.
- `script.js` — логика фильтрации, рандомного факта, переключателя тем.
- `facts.js` — данные фактов (title, текст, категории).
- `categories.js` — список категорий с иконками и русскими подписями.

## Как запустить
1) Открой `index.html` в браузере (дважды кликнуть или через `File -> Open`).  
2) Всё работает сразу, интернет не нужен.

## Управление
- Поиск по фактам — поле под заголовком.
- Категории — чипы ниже, одиночный выбор + кнопка «Все».
- Рандомный факт — кнопка 🔀 в карточке справа.
- Тема — иконка 🌙/🌤 в шапке справа.

## Примечания
- Данные лежат в `facts.js`, редактируются вручную (формат: `title`, `text`, `categories`).
- Категории и иконки — `categories.js` (id на английском, label на русском).
